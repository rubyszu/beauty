SuccessResponse:
  description: 请求成功的通用Json返回
  type: object
  properties:
    code:
      type: integer
      format: int32
      enum:
        - 200
    errcode:
      type: string
      enum:
        - OK
    type:
      type: string
      enum:
        - OK
  required:
    - code
    - errcode
    - type