UnknownError:
  description: Unknown Error
  type: object
  properties:
    errcode:
      type: string
      enum:
        - Unknown
    code:
      type: integer
      format: int32
      enum:
        - 500
    type:
      type: string
      enum:
        - Unknown
  required:
    - errcode
    - code
    - type